services:
  fastapi_app:
    image: iucd/gin-mt:latest
    container_name: gin-mt
    user: "1000:1000"
    ports:
      - "8080:8080"
    environment:
      - LOG_LEVEL=info
    command: ["./app"]
    volumes:
      - ./app_data:/app_data
    restart: unless-stopped