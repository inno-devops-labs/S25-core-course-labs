version: '3.8'

services:
  moscow-time:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data  # Persistent storage for visit counter
    environment:
      - FLASK_ENV=production