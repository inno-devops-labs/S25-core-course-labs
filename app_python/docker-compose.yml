version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5001:5000"
    volumes:
      - ./visits:/app/visits
    environment:
      - FLASK_DEBUG=0
