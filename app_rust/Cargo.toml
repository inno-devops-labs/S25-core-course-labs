[package]
name = "app_rust"
version = "0.1.0"
edition = "2021"

[dependencies]
rocket = "0.5.1"
chrono = "0.4"
prometheus = { version = "0.13", features = ["process"] }
lazy_static = "1.4.0"

[dev-dependencies]
regex = "1.11.1"
