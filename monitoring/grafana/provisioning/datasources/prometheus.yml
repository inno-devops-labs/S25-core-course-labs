apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    uid: prometheus
    version: 1
    editable: false
    jsonData:
      timeInterval: "5s" 