replicaCount: 1

image:
  repository: paranid5/app_flutter
  pullPolicy: IfNotPresent
  tag: "latest"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  automount: true
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {flutter}

podSecurityContext: {}

securityContext: {}

service:
  type: NodePort
  port: 8081
  targetPort: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: paranid5.app_flutter.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []

resources: {}

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80

volumes:
  - name: config-volume
    configMap:
      name: app-config

volumeMounts:
  - name: config-volume
    mountPath: "/data/config.json"
    subPath: "config.json"
    readOnly: true

nodeSelector: {}

tolerations: []

affinity: {}

config:
  path: "files/config.json"
