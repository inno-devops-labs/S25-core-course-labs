apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-flutter.fullname" . }}
  labels:
    {{- include "app-flutter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-flutter.selectorLabels" . | nindent 4 }}
