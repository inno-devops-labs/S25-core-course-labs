apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.statefulSet.serviceName }}
  labels:
    {{- include "chart-2.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    {{- include "chart-2.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http