apiVersion: v1
kind: Service
metadata:
  name: app-stateful-external
  namespace: stateful-lab
  labels:
    app: app-stateful
spec:
  type: NodePort
  ports:
    - port: 5000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: app-stateful 