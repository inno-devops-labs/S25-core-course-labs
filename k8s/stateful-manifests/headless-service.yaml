apiVersion: v1
kind: Service
metadata:
  name: app-stateful
  namespace: stateful-lab
  labels:
    app: app-stateful
spec:
  clusterIP: None
  ports:
    - port: 5000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: app-stateful 