apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-apps.fullname" . }}
  labels:
    {{- include "web-apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "web-apps.selectorLabels" . | nindent 4 }}
