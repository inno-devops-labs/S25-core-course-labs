apiVersion: v1
kind: Secret
metadata:
  name: my-secret
  labels:
    app: flask-app
type: Opaque
data:
  username: {{ .Values.secrets.username | b64enc }}
  password: {{ .Values.secrets.password | b64enc }} 
