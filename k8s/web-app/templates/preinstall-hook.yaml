apiVersion: v1
kind: Pod
metadata:
  name: "preinstall"
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  containers:
  - name: preinstall
    image: busybox
    command: ["sleep", "20"]
  restartPolicy: Never