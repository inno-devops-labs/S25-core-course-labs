apiVersion: v1
kind: Service
metadata:
  name: {{ include "fastapi-mt.fullname" . }}
  labels:
    {{- include "fastapi-mt.labels" . | nindent 4 }}
spec:
  clusterIP: None
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fastapi-mt.selectorLabels" . | nindent 4 }}
