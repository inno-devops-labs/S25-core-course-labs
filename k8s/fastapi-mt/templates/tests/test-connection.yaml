apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastapi-mt.fullname" . }}-test-connection"
  labels:
    {{- include "fastapi-mt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastapi-mt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
