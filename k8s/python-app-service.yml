apiVersion: v1
kind: Service
metadata:
  name: my-django-app
spec:
  selector:
    app: my-django-app
  ports:
    - name: my-django-app
      protocol: TCP
      port: 8082
      targetPort: 8000
  type: NodePort