apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-golang.fullname" . }}
  labels:
    {{- include "app-golang.labels" . | nindent 4 }}
spec:
  ports:
    - name: http
      port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "app-golang.selectorLabels" . | nindent 4 }}