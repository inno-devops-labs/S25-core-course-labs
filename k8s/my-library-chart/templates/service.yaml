apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-library-chart.fullname" . }}
  labels:
    {{- include "my-library-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-library-chart.selectorLabels" . | nindent 4 }}
