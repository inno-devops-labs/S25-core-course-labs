apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gin-mt.fullname" . }}-test-connection"
  labels:
    {{- include "gin-mt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gin-mt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
