apiVersion: v1
kind: Service
metadata:
  name: {{ include "gin-mt.fullname" . }}
  labels:
    {{- include "gin-mt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gin-mt.selectorLabels" . | nindent 4 }}
