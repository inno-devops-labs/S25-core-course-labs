apiVersion: v1
kind: Service
metadata:
  name: app-python-service
  namespace: default
spec:
  selector:
    app: app-python
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000