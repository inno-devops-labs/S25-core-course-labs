apiVersion: v1
kind: Service
metadata:
  name: watch-service
spec:
  type: NodePort
  selector:
    app: watch
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30000  # Not a best way, but for now we do not have a balancer on edge of the k8s network
