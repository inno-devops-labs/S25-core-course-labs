apiVersion: v1
kind: Secret
metadata:
  name: credentials
  labels:
    app: app-piton
    chart: '{{ .Chart.Name }}-{{ .Chart.Version }}'
    release: '{{ .Release.Name }}'
    heritage: '{{ .Release.Service }}'
type: Opaque
data:
  telegram_id: {{ .Values.telegram_id | b64enc | quote }}