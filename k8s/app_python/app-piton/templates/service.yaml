apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-piton.fullname" . }}
  labels:
    {{- include "app-piton.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-piton.selectorLabels" . | nindent 4 }}
