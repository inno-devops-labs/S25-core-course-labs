apiVersion: v1
kind: Service
metadata:
  name: moscow-time-service
spec:
  selector:
    app: moscow-time-container
  ports:
    - protocol: TCP
      port: 8000        
      targetPort: 8000  
  type: NodePort
