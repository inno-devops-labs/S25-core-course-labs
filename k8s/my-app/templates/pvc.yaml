apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-app-pvc
  labels:
    {{- include "my-app.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
