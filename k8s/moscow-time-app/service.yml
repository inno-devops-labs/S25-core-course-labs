apiVersion: v1
kind: Service
metadata:
  name: moscow-time-app
spec:
  clusterIP: None   # <-- Это делает сервис headless, важно для DNS подов
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
  selector:
    app: moscow-time-app
