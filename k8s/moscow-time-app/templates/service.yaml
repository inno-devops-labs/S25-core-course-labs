apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  clusterIP: None  # Makes it headless
  ports:
  - port: {{ .Values.containerPort }}
    targetPort: {{ .Values.containerPort }}
  selector:
    app: moscow-time-app