apiVersion: v1
kind: Service
metadata:
  name: flask-app
spec:
  selector:
    app: flask-app-app
  type: NodePort  
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007