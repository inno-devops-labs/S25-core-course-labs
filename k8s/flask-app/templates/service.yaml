apiVersion: v1
kind: Service
metadata:
  name: flask-app
spec:
  selector:
    app: flask-app-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort
  nodePort: 30007
