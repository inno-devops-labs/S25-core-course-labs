replicaCount: 2  # Higher replicas for production

image:
  repository: my-python-app
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 80
  nodePort: 30007  # Explicit nodePort for external access

env: prod
