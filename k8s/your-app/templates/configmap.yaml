apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "your-app.fullname" . }}-config  # Replace `<CHARTNAME>` with your actual chart name
data:
  config.json: |-
{{ .Files.Get "files/config.json" | indent 4 }}
