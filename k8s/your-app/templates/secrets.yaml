
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-db-secret
  labels:
    app: flask-app
type: Opaque
data:
  db-password: {{ .Values.dbPassword | b64enc | quote }}  # Helm auto-base64 encodes