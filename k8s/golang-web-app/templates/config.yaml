apiVersion: v1
kind: ConfigMap
metadata:
  name: golang-web-app-config
  labels:
    {{- include "golang-web-app.labels" . | nindent 4 }}
data:
  config.json: |-
    {{ .Files.Get "files/config.json" | nindent 4 }}
