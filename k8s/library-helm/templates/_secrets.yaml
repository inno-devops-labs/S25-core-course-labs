{{- define "library-helm.common-secret" -}}
apiVersion: v1
kind: Secret
metadata:
   name: {{ printf "common-secret-%s" .Chart.Name }}
type: Opaque
data:
  MY_PASS: {{ .Values.MY_PASS | b64enc | quote }}
{{- end -}}
