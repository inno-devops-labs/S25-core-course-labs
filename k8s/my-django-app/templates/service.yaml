apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-django-app.fullname" . }}
  labels:
    {{- include "my-django-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-django-app.selectorLabels" . | nindent 4 }}
