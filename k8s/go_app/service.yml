apiVersion: v1
kind: Service
metadata:
  name: go-moscow-time-service
spec:
  selector:
    app: go-moscow-time
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer