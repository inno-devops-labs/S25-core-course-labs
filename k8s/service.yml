apiVersion: v1
kind: Service
metadata:
  name: fastapi-mt-service
spec:
  type: NodePort
  selector:
    app: fastapi-mt
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30000