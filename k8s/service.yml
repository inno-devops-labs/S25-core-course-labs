apiVersion: v1
kind: Service
metadata:
  name: python-web
  labels:
    app: python-web
spec:
  type: LoadBalancer
  selector:
    app: python-web
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000