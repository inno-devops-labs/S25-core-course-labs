apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}-my-chart
spec:
  selector:
    app: msk-time
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: NodePort

