apiVersion: v1
kind: Service
metadata:
  name: python-time-app-service
spec:
  selector:
    app: python-time-app
  ports:
    - name: python-time-app-service
      protocol: TCP
      port: 8082
      targetPort: 5000
      nodePort: 30002
  type: NodePort