apiVersion: v1
kind: Secret
metadata:
  name: {{ include "test-app.fullname" . }}-secret
  labels:
    {{- include "test-app.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }} 