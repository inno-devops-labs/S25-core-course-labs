apiVersion: v1
kind: Pod
metadata:
  name: post-install-hook
  annotations:
    "helm.sh/hook": post-install
spec:
  containers:
    - name: post-install-container
      image: busybox
      command: ["sh", "-c", "sleep 20"]
  restartPolicy: Never