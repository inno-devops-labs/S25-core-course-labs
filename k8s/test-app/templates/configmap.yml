apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "test-app.fullname" . }}-config
  labels:
    {{- include "test-app.labels" . | nindent 4 }}
data:
  config.json: |
    {
      "username": "admin",
      "password": "password123"
    }