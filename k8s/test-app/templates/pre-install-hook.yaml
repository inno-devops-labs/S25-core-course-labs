apiVersion: v1
kind: Pod
metadata:
  name: pre-install-hook
  annotations:
    "helm.sh/hook": pre-install
spec:
  containers:
    - name: pre-install-container
      image: busybox
      command: ["sh", "-c", "sleep 20"]
  restartPolicy: Never