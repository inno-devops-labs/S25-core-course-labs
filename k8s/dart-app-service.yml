apiVersion: v1
kind: Service
metadata:
  name: my-dart-app
spec:
  selector:
    app: my-dart-app
  ports:
    - name: my-dart-app
      protocol: TCP
      port: 8081
      targetPort: 80
  type: NodePort