## Mount a Config File

```bash
> helm upgrade --install app-python ./app-python
```
```bash
Release "app-python" has been upgraded. Happy Helming!
NAME: app-python
LAST DEPLOYED: Fri Mar 14 21:52:38 2025
NAMESPACE: default
STATUS: deployed
REVISION: 12
NOTES:
1. Get the application URL by running these commands:
  export POD_NAME=$(kubectl get pods --namespace default -l "app.kubernetes.io/name=app-python,app.kubernetes.io/instance=app-python" -o jsonpath="{.items[0].metadata.name}")
  export CONTAINER_PORT=$(kubectl get pod --namespace default $POD_NAME -o jsonpath="{.spec.containers[0].ports[0].containerPort}")
  echo "Visit http://127.0.0.1:8080 to use your application"
  kubectl --namespace default port-forward $POD_NAME 8080:$CONTAINER_PORT
```
```bash
> kubectl get po
```
```bash
NAME                                     READY   STATUS    RESTARTS      AGE
app-python-795b46d7d7-vrxs7              1/1     Running   0             87s
helm-hooks-app-python-5b7575787d-8k97x   1/1     Running   1 (21h ago)   22h
```
```bash
> kubectl exec app-python-795b46d7d7-vrxs7 -- cat /config.json
```
```bash
{
  "app_name": "python-web-app",
  "message": "Welcome!",
  "maintainer": "Olesia Grediushko"
}
```