apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-nodejs.fullname" . }}
  labels:
    {{- include "app-nodejs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-nodejs.selectorLabels" . | nindent 4 }}
