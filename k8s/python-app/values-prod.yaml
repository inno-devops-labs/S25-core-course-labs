# Production environment configuration
replicaCount: 2

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 250m
    memory: 256Mi

env:
  appSecret: "prod-secret-key"
  appEnv: "production"

persistence:
  enabled: true
  size: 5Gi  # More storage for production