replicaCount: 1

image:
  repository: em1999jay/python-app
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 5000

env:
  FLASK_ENV: development
  DEBUG: "true"

resources:
  requests:
    memory: "64Mi"
    cpu: "250m"
  limits:
    memory: "128Mi"
    cpu: "500m"
