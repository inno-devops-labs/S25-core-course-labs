apiVersion: v1
kind: Service
metadata:
  name: python-app-service
spec:
  selector:
    app.kubernetes.io/name: python-app
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
