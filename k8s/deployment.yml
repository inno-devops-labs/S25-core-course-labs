apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-python
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app-python
  template:
    metadata:
      labels:
        app: app-python
    spec:
      containers:
        - name: app-python
          image: daniilzimin4/devopsapp:latest
          ports:
            - containerPort: 9200
          env:
            - name: MY_PASS
              valueFrom:
                secretKeyRef:
                  name: python-app-secret
                  key: MY_PASS