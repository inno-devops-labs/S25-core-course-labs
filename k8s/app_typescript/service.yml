apiVersion: v1
kind: Service
metadata:
  name: app-typescript-service
  namespace: default
spec:
  selector:
    app: app-typescript
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080