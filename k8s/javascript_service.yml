apiVersion: v1
kind: Service
metadata:
  name: app-javascript-service
spec:
  selector:
    app: app-javascript
  type: NodePort
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer
