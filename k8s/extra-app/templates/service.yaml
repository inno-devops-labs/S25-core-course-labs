apiVersion: v1
kind: Service
metadata:
  name: {{ include "extra-app.fullname" . }}
  labels:
    {{- include "extra-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "extra-app.selectorLabels" . | nindent 4 }}
