apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "extra-app.fullname" . }}-test-connection"
  labels:
    {{- include "extra-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "extra-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
