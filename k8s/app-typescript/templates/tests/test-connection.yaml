apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-typescript.fullname" . }}-test-connection"
  labels:
    {{- include "app-typescript.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-typescript.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
