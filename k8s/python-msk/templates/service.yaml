apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-msk.fullname" . }}
  labels:
    {{- include "python-msk.labels" . | nindent 4 }}
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 5000
  selector:
    app: my-release-python-msk
