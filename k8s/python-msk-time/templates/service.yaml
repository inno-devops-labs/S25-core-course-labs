apiVersion: v1
kind: Service
metadata:
  name: {{ include "python_msk_time.fullname" . }}
  labels:
    {{- include "python_msk_time.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python_msk_time.selectorLabels" . | nindent 4 }}
