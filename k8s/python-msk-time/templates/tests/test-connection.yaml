apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "python_msk_time.fullname" . }}-test-connection"
  labels:
    {{- include "python_msk_time.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "python_msk_time.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
