apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-app-go.fullname" . }}
  labels:
    {{- include "chart-app-go.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-app-go.selectorLabels" . | nindent 4 }}
