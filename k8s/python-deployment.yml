apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-web-app-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: python-web-app
  template:
    metadata:
      labels:
        app: python-web-app
    spec:
      containers:
      - name: python-web-app
        image: lekski/python-web-app
        ports:
        - containerPort: 8000