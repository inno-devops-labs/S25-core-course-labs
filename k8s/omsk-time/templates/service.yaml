apiVersion: v1
kind: Service
metadata:
  name: {{ include "omsk-time.fullname" . }}
  labels:
    {{- include "omsk-time.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "omsk-time.selectorLabels" . | nindent 4 }}
