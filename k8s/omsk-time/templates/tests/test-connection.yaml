apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "omsk-time.fullname" . }}-test-connection"
  labels:
    {{- include "omsk-time.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "omsk-time.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
