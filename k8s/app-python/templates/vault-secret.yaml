{{- if .Values.vault.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "app-python.fullname" . }}-vault-secret
  labels:
    vault-inject: "true"
stringData:
  MY_VAULT_PASS: "1221"
{{- end }}