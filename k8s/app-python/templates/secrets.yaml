apiVersion: v1
kind: Secret
metadata:
  name: my-vault-secret
  labels:
    app: vault
type: Opaque
data:
  username: {{ .Values.vaultSecrets.username | b64enc | quote }}
  password: {{ .Values.vaultSecrets.password | b64enc | quote }}

