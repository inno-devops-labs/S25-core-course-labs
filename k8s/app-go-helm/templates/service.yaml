apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-go-helm.fullname" . }}
  labels:
    {{- include "app-go-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app-go-helm.selectorLabels" . | nindent 4 }}
