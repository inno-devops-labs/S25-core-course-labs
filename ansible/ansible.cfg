[defaults]
inventory           = inventory
remote_user         = askar
host_key_checking   = False
private_key_file    = /home/askar/.ssh/id_rsa

[inventory]
any_unparsed_is_failed = True

[privilege_escalation]
become = True
become_user = root
become_method = sudo

[global]
break-system-packages = true