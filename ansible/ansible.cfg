[defaults]
playbook_dir = playbooks/
roles_path = roles/

inventory = inventory/yacloud_compute.yml
inventory_plugins = plugins/inventory/

remote_user = yc_user

host_key_checking = false
private_key_file = $HOME/.ssh/id_yc

[inventory]
enable_plugins = yacloud_compute