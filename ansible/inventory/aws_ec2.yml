---
plugin: aws_ec2
regions:
  - eu-central-1
filters:
  tag:Environment:
    - dev
    - staging
keyed_groups:
  - key: tags.Environment
    prefix: env
  - key: tags.Role
    prefix: role
compose:
  ansible_host: public_ip_address 