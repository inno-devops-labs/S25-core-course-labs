---
- hosts: localhost
  connection: local
  become: yes
  roles:
    - role: web-app
      tags:
        - deploy
  tasks:
    - name: Include wipe tasks
      include_tasks: roles/web_app/tasks/0-wipe.yml
      when: web_app_full_wipe | bool
      tags:
        - wipe
