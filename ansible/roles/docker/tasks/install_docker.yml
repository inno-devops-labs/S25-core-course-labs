- name: Install Docker
  apt:
    name: docker.io
    state: present
  become: yes

- name: Start and enable Docker service
  systemd:
    name: docker
    enabled: yes
    state: started
  become: yes

