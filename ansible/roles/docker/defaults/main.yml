docker_users:
  - "{{ ansible_user }}" 

docker_packages: # From official instruction. Only works good for apt-based systems.
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin
