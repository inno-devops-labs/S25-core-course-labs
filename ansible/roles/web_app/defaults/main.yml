---
# Docker image settings
docker_image: dantetemplar/moscow-time-webapp:latest
app_port: 8000  # Changed to match the Dockerfile EXPOSE port

# Wipe settings
web_app_full_wipe: false

# Application settings
app_name: moscow_time_webapp
container_name: "{{ app_name }}_container"