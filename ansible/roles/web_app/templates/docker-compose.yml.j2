version: '3'
services:
  app:
    image: "{{ python_web_app_docker_image_name }}:{{ python_web_app_version }}"
    ports:
      - "{{ python_web_app_internal_port }}:{{ python_web_app_external_port }}"