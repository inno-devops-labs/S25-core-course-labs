services:
  {{ docker_container }}:
    image: "{{ docker_image }}"
    container_name: "{{ docker_container }}"
    ports:
      - "{{ external_port }}:{{ internal_port }}"
    restart: unless-stopped