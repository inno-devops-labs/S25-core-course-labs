# roles/web_app/tasks/0-wipe.yml
- name: Remove old Docker containers
  docker_container:
    name: "{{ item }}"
    state: absent
  loop:
    - app_python
    - app_javascript
  when: web_app_full_wipe | default(false)
  tags:
    - wipe
