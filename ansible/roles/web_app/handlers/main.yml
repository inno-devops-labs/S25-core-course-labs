---
- name: restart web application
  command: docker-compose -f {{ web_app_data_dir }}/docker-compose.yml down && docker-compose -f {{ web_app_data_dir }}/docker-compose.yml up -d
  args:
    chdir: "{{ web_app_data_dir }}" 