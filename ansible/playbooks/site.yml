---
- name: Deploy web application
  hosts: all
  become: true  # Используем sudo для выполнения команд
  roles:
    - role: web_app
      vars:
        docker_image: "grisharybolovlev/fastapi-app-python:latest"
        app_port: 8000 