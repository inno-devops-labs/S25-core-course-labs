- name: Deploy Web App
  hosts: all
  become: true
  roles:
    - role: docker
    - role: web_app
      vars:
        web_app_name: "app_python"
        web_app_external_port: 5001
        web_app_internal_port: 5000
        web_app_dir: "~/app_python"
