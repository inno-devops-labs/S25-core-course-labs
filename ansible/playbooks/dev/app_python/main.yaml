---
- name: Deploy Python Web Application
  hosts: all
  become: true
  
  vars:
    app_directory: /opt/python_app
    docker_image: "{{ lookup('env', 'DOCKERHUB_USERNAME') }}/python-moscow-time:latest"
    app_port: 3000

  roles:
    - role: web_app