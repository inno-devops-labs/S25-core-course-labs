- hosts: all
  become: true
  name: Deploy Golang application
  roles:
    - role: web_app
      vars:
        docker_image_name: dnworks/app_golang
        docker_image_tag: latest
        docker_app_port: 80
        docker_container_name: app_golang
        docker_container_port: 80
