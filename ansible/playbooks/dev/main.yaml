---
- name: Install Docker and Deploy Web App
  hosts: localhost
  become: yes
  tasks:
    - name: Install Docker
      include_role:
        name: docker

    - name: Pull and deploy Docker image for the web app
      include_role:
        name: web_app
