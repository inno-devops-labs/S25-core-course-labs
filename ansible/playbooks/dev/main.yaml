---
- hosts: all
  become: true

  roles:
    - role: geerlingguy.docker
      vars:
        docker_edition: "ce"
        docker_version: "latest"
        docker_package: "docker-ce"
    - role: web_app
