---
- name: Deploy Java app
  hosts: docker_hosts
  become: true
  roles:
    - role: web_app
      vars:
        app_image_name: "catorleader/vacation_calculator"
        app_image_tag: "distroless"
        app_container_name: "vacation_calculator"
        container_port: "8000"
        host_port: "8000"
        app_full_wipe: true
