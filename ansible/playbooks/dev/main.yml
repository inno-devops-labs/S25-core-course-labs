---
# - name: Install Docker and Docker Compose
#   hosts: localhost
#   become: yes
#   roles:
#     - geerlingguy.docker  # The installed Docker role

- name: Install Docker and Docker Compose
  hosts: all
  become: yes
  roles:
    - docker
