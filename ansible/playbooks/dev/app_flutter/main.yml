- name: Deploy Web App
  hosts: all
  become: true
  roles:
    - role: docker
    - role: web_app
      vars:
        app_name: "app_flutter"
        app_external_port: 8877
        app_internal_port: 80
        app_dir: "~/app_dart"
